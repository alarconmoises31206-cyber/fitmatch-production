const { createClient } = require("@supabase/supabase-js"); const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL; const supabaseKey = process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY; const supabase = createClient(supabaseUrl, supabaseKey); (async () => { const { data, error } = await supabase.from("trainer_profiles").select("*").limit(1); if (error) console.error(error); else { console.log("Columns:", Object.keys(data[0])); console.log("Has vector_embedding?", "vector_embedding" in data[0]); } })();
