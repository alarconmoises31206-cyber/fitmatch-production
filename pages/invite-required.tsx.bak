import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { HomeIcon } from 'lucide-react';
import Link from 'next/link';
import React from 'react';

export default function InviteRequiredPage() {
  return (
    <div className=\"flex items-center justify-center min-h-screen bg-background\">
      <Card className=\"w-full max-w-md\">
        <CardHeader>
          <CardTitle className=\"text-2xl\">Invite Required</CardTitle>
          <CardDescription>
            Access to FitMatch Director is currently restricted to invited organizations.
          </CardDescription>
        </CardHeader>
        <CardContent className=\"space-y-4\">
          <p className=\"text-sm text-muted-foreground\">
            If you have an invite link, please click it directly. If you believe you should have access,
            please contact your organization’s administrator or our support team.
          </p>
          <div className=\"flex flex-col space-y-2\">
            <Button asChild>
              <Link href=\"/\">
                <HomeIcon className=\"mr-2 h-4 w-4\" />
                Return to Homepage
              </Link>
            </Button>
            <Button variant=\"outline\" asChild>
              <a href=\"mailto:support@fitmatch.com\">Contact Support</a>
            </Button>
          </div>
        </CardContent>
      </Card>
    </div>
  )
}
